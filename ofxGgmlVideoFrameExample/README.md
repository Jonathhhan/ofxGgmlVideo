# ofxGgmlVideoFrameExample

Root-level montage clip/frame smoke example for `ofxGgmlVideo`. It keeps the
first workflow intentionally small by planning deterministic montage segments
and frame samples that can later be handed to an agent, vision scorer, or CLIP
scorer owned by another addon.
